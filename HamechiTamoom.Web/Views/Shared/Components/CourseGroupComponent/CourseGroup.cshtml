@model IEnumerable<HamechiTamoom.DataLayer.Entities.Course.CourseGroup>
    
    <div class="main_menu_area">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-3 col-md-12">
                    <div class="categories_menu">
                        <div class="categories_title">
                            <h2>دسته‌بندی ها</h2>
                        </div>
                        <div class="categories_menu_toggle">
                            <ul>
                                @foreach (var group in Model.Where(p => p.ParrentId == null))
                                {
                                <li class="menu_item_children">
                                    <a href="/Course?SelectedGroups=@group.GroupId">@group.GroupTitle <i class="fa fa-angle-left"></i></a>
                                    @if (Model.Any(g => g.ParrentId == group.GroupId))
                                    {
                                    <ul class="categories_mega_menu">
                                        @foreach (var sub in Model.Where(g => g.ParrentId == group.GroupId))
                                        {
                                        <li class="menu_item_children">
                                            <a href="/Course?SelectedGroups=@sub.GroupId">@sub.GroupTitle</a>
                                            @if (Model.Any(g => g.ParrentId == sub.GroupId))
                                            {
                                            <ul class="categorie_sub_menu">
                                                @foreach (var sub2 in Model.Where(s => s.ParrentId == sub.GroupId))
                                                {
                                                <li class="menu_item_children">
                                                    <a href="/Course?SelectedGroups=@sub2.GroupId">@sub2.GroupTitle</a>
                                                </li>
                                                }
                                            </ul>
                                            }
                                        </li>
                                        }
                                    </ul>
                                    }
                                </li>
                                }
                            </ul>
                        </div>
                    </div>
                </div>
                <div class="col-lg-9 col-md-12">
                    <div class="main_menu menu_position">
                        <nav>
                            <ul>
                                <li><a href="/AboutUs">درباره همه چی تموم</a></li>
                                <li><a href="/ContactUs"> تماس با ما</a></li>
                            </ul>
                        </nav>
                    </div>
                </div>
            </div>
        </div>
    </div>
